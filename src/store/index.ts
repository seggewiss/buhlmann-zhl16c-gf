import { createStore } from 'vuex'

export default createStore({
  state: {
    depth: 0,
    diveTime: 0
  },
  mutations: {
    // Todo add descent ascent addDiveTime etc.
  },
  actions: {
  },
  modules: {
  }
})
